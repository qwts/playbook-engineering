node_modules
issues
.DS_Store
.vscode
.idea
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.development
.env.test
.env.production
.claude/settings.local.json
# Legacy agent-guard run journal (#239). The wrapper no longer writes here —
# diagnostics moved under the machine state directory — but checkouts that ran
# the previous wrapper still have `.guard/` on disk. Keep it ignored so a
# routine `git add -A` cannot stage stale machine-local telemetry; safe to
# delete the directory once seen.
.guard/
